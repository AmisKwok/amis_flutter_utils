name: amis_utils
description: A collection of utility classes for Flutter applications. Available on Gitee and GitHub.
version: 0.1.0
homepage: https://github.com/AmisKwok/amis_flutter_utils.git

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  logger: ^2.0.2+1
  encrypt: ^5.0.3
  flutter_dotenv: ^6.0.0
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0