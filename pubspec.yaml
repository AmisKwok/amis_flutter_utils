name: amis_utils
description: A collection of utility classes for Flutter applications
version: 0.1.0
homepage: https://github.com/yourusername/vibe_utils

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0
  logger: ^2.0.2
  encrypt: ^5.0.1
  flutter_dotenv: ^5.1.0
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0